# Ownership

[Документация](../index.md) · [Словарь](../glossary.md) · [Memory](../memory/index.md) · [Representations](../representations.md)

Efen разделяет право владения ресурсом и права доступа к нему. Система типов
проверяет эти права во время компиляции.

## Аспекты владения

| Аспект | Право |
|---|---|
| `own` | уничтожить значение или передать обязанность уничтожения |
| `read` | читать содержимое |
| `shared` | использовать право в нескольких потоках по правилам типа |
| `write` | изменять содержимое |
| `split` | выделять независимые подвладения |
| `inspect` | видеть идентичность без доступа к содержимому |

Владеющее поле выражается через `own`; компилятор выводит связь жизни значения с
содержащим его местом из контекста и не отключает проверки перемещения.

`mut` является сокращением эксклюзивного `write` без `shared` и не создаёт
седьмого права.

`managed` не является правом типа. Это режим конкретного слота, в котором
borrow checker отключён, а операции времени жизни строятся выбранной моделью
управления. Один и тот же тип может одновременно находиться в обычном слоте с
borrow-дисциплиной и в `managed`-слоте без неё. Слоты объектов под ARC являются
`managed`; наличие того же типа в другом слоте само по себе не переносит режим.

В исходной программе нет модификатора `managed`. Режим устанавливает layout,
аспект или метакод при построении HIR.

Режим не выбирает конкретный manager и не ограничивает управление аспектом
класса. Аспект, метакод или другое расширение компилятора может самостоятельно
построить создание, хранение, присваивание, копирование, перемещение и
освобождение значения. ARC, GC и arena являются возможными библиотечными
моделями, а не закрытым перечнем механизмов Efen.

Layout может использовать `managed` для скрытых физических slots своих columns
и mappings. Это не отключает проверку публичного логического place: ссылка на
элемент или поле населения сохраняет origin, права и живость и блокирует
конфликтующее удаление или перемещение. Внутренний режим backing storage не
переносится на пользовательское связывание.

## Слоты и связывания

Слот представляет место, в котором находится ресурс, несёт объявленные права и
режим управления. Имена могут ссылаться на один слот; в обычном слоте передача
`own` через одно имя делает прежнее владеющее употребление через остальные
имена недопустимым. Для `managed`-слота borrow checker это состояние не ведёт.

Обычное неизменяемое связывание получает `own + read`. Оно также получает
`write`, если сам тип допускает изменение:

```efen
let file: FileHandle
```

`let` запрещает перепривязку имени, но не обязательно изменение объекта.
Readonly-тип не даёт `write`. CoW-тип при записи сначала выполняет предусмотренное
типом отделение копии.

## Параметры

Параметр без ownership-аннотации не получает `own`: обязанность освободить
значение остаётся у вызывающего. Анализ тела начинает с максимально широких
не-владеющих прав и сужает их до реально использованных.

```efen
fn checksum -> UInt64 {
    param buffer: Buffer

    // Итоговая сигнатура получает только реально потребовавшиеся права.
}
```

Выведенный набор прав является частью сигнатуры и экспортируется в записи
символа. Изменение тела, потребовавшее более широких прав, изменяет публичный
контракт и инвалидирует зависимые вызовы.

Явная передача владения остаётся частью сигнатуры:

```efen
fn consume {
    param file: FileHandle own

    // функция принимает обязанность уничтожить file
}
```

## Ссылки

Ссылка не получает `own`. Ссылочная форма и доступные через неё права являются
частью статического типа, поэтому такой тип можно использовать в результате,
поле, alias и generic-аргументе.

`ref` является полной словесной формой невладеющей ссылки, а `&` — её коротким
алиасом:

```efen
ref T             // то же, что &T
ref read T        // то же, что &read T
ref out T         // то же, что &out T
```

`ref` определяет ссылочную форму, `read`/`write`/`out` — доступ, а `own`
остаётся отдельным правом владеть значением и уничтожить его. Действие создания
временной ссылки может называться `borrow`; оно не является названием типа.

| Форма | Доступ через ссылку |
|---|---|
| `&T` | чтение и запись |
| `&read T` | только чтение |
| `&out T` | только запись |

Optional не меняет режим ссылки: `&T?` — читающая и пишущая ссылка на
optional-место, а `&out T?` — только пишущая.

Право, записанное в типе значения, не определяет изменяемость содержащего его
слота. Например:

```efen
var tail: read Items.Item? = null
```

`var` разрешает переприсваивать поле `tail`. `read` относится к хранимому
типизированному указателю `Items.Item`: сам указатель не получает владение целью
и предоставляет читающий доступ. Это не превращает `tail` в `let`.

Права из разных доказанных источников могут использоваться вместе. Внутри
операции, имеющей отдельное `write`-право на соответствующий layout или элемент,
указатель из `tail` определяет цель, а это отдельное право разрешает изменить её
поле. Поэтому `tail!.next = value` допустимо при наличии такого `write`-права.
Один лишь `read Items.Item` права записи не создаёт и не преобразуется в него.

```efen
fn increment {
    param value: &Int

    value = value + 1
}

fn inspect {
    param value: &read Int

    echo value
}

fn initialize {
    param value: &out Int

    value = 0
}
```

`&read T` запрещает запись только через эту ссылку. Он не обещает глобальную
неизменность объекта; другой законный владелец или ссылка может изменить его.
Параллельный доступ отдельно регулирует `shared`.

Происхождение возвращаемой ссылки является частью её типа и при необходимости
указывается после режима доступа. Полная и короткая формы равнозначны:

```efen
// Полная форма результата: ref read[values] Int
// Короткая форма результата: &read[values] Int
fn first -> &read[values] Int {
    param values: &read Array<Int>

    return &read values[0]
}
```

`[values]` означает, что результат происходит из места, доступного через
`values`, и не может оставаться годным после окончания или инвалидации этого
origin. Несколько возможных источников перечисляются вместе:

```efen
fn choose -> &read[left, right] Int {
    param left: &read Int
    param right: &read Int
    param condition: Bool
}
```

Origin можно опустить, когда компилятор однозначно выводит его из всех путей
возврата. Выведенная связь сохраняется в сигнатуре так же, как выведенные права:

```efen
fn first -> &read Int {
    param values: &read Array<Int>
}
```

Если origin вывести нельзя, отсутствие `[origin]` является ошибкой, а не
неограниченной ссылкой.

## Хранимые и захваченные ссылки

Ссылку можно хранить в обычном классе или структуре, если её тип называет origin
и компилятор доказывает, что origin живёт не меньше содержащего значения:

```efen
struct View {
    generic T: Type
    generic Source: Origin
    let value: &read[Source] T
}
```

Origin является частью типа `View`. Значение нельзя вернуть, сохранить или
передать дальше границы, после которой `Source` может стать недействительным.
То же правило применяется к ссылке, захваченной замыканием или корутиной:
время жизни origin обязано покрывать время жизни среды захвата. Передача такой
среды в другой поток дополнительно требует права `shared`; она не продлевает
origin автоматически.

Хранимая ссылка на поле того же перемещаемого объекта этим правилом
автоматически не разрешается: перемещение способно изменить адрес поля. Такой
тип требует отдельно гарантированного стабильного storage.

## Права как часть типа

Полное типовое выражение включает базовый тип, ссылочную форму и права. Поэтому
generic-параметр `T: Type` может получить каждую из форм:

```efen
User
&read User
&User
User shared read
User inspect
```

Один generic-компонент может работать с ними без отдельного объявления:

```efen
fn identity -> T {
    generic T: Type
    param value: T

    return value
}
```

Допустимые операции определяются правами фактического `T`. Текущий набор прав
конкретного слота может сужаться вдоль потока управления, например после
передачи `own`; это уточнение типа в точке программы.

## Явное извлечение

`take` передаёт `own` из optional-свойства и записывает туда `null`:

```efen
class Queue {
    own var pending: Job?

    fn detach -> Job? {
        return take self.pending
    }
}
```

Обычное чтение владеющего поля не копирует и не перемещает значение: оно даёт
заимствование с правами, допустимыми в точке доступа. Передача владения из поля
всегда требует явного `take`:

```efen
let pointer = node.pointer       // заимствование
let pointer = take node.pointer  // передача own
```

Compiler-known операция `replace(place, value)` сама является явным действием
передачи владения: она потребляет владеющий второй аргумент, помещает его в
`place` и возвращает прежнее владеющее значение. Поэтому отдельный `take value`
в этой форме не пишется:

```efen
let previous = replace(item.Target, value)
```

Это узкая семантика `replace`, а не общее разрешение неявно передавать `own` в
обычные вызовы.

`take` целой локальной переменной разрешён для `Movable`, пока нет живого
заимствования. После перемещения `let` больше нельзя использовать; `var` снова
становится доступен после присваивания нового значения. Анализ CFG требует
инициализации на каждом входящем пути, а деструктор не вызывается для уже
перемещённого значения.

Индексированное место можно извлечь только тогда, когда у него есть определённое
пустое состояние. Для элемента `T?` операция записывает `null`. Обычный элемент
плотной коллекции извлекается её собственной операцией удаления, поскольку
произвольный `take collection[index]` оставил бы в коллекции несуществующий
элемент.

Возможность физически перенести значение задаёт contract `Movable`. Она не
следует из `Copyable`: явная копия создаёт новый экземпляр, а `take` передаёт
существующий. `ImplicitlyCopyable` наследует только `Copyable` и также не даёт
`Movable`.

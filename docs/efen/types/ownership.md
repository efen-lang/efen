# Ownership

`Efen` старается описать на высоком уровне абстракции все возможные модели владения ресурсами, 
которые могут быть полезны в различных приложениях.

Различаются следующие аспекты владения:
- Право вызова деструктора. Кто имеет право освободить ресурс.
- Право доступа на чтение. Кто может читать ресурс.
- Право доступа на запись. Кто может изменять ресурс.
- Право на разделение владения (создание разных областей памяти).
- Право на чтение ссылки (аналог tag в Pony).

| № | Аспект владения                       | Атрибут   | Описание                                                          |
|---|---------------------------------------|-----------|-------------------------------------------------------------------|
| 1 | Право вызова деструктора              | `own`     | Переменная владеет ресурсом и управляет его временем жизни.       |
| 2 | Право доступа на чтение               | `read`    | Разрешено читать содержимое ресурса.                              |
| 3 | Право на параллельное чтение/запись   | `shared`  | Разрешено параллельно читать или писать ресурс из разных потоков. |
| 4 | Право доступа на запись               | `write`   | Разрешено изменять содержимое ресурса.                            |
| 5 | Право на разделение владения          | `split`   | Разделение на под-владения.                                       |
| 6 | Право на чтение ссылки (идентичности) | `inspect` | Разрешено видеть идентичность ресурса без доступа к данным.       |

Без права `own` ресурс не может быть освобожден автоматически ни при каких условиях.

Атрибут `mut` (эксклюзивная запись) является алиасом для права `write`, но без `shared` доступа.

Между аспектами существуют следующие зависимости:
- `own` может потерять право чтения и/или записи, что называется "неполным владением".
- `read` может быть понижено до `inspect`.
- `write` всегда требует наличия `read`.
- `split` может быть как с `read` так и с `write`.
- `inspect` не требует никаких других прав.

Атрибут `shared` может комбинироваться с `read` и/или `write`, позволяя параллельный доступ к ресурсу из разных потоков.
Атрибут `split` позволяет создавать под-владения ресурса, которые могут иметь свои собственные аспекты владения.
Он может комбинироваться с `read` и/или `write`.

Правила понижения прав:
- `own` → `read` или `write`
- `read` → `inspect`
- `write` → `read`
- `write` -> `split`

Право эксклюзивной записи (`mut`) лишает владельца права на чтение или запись до тех пор, 
пока `mut` право не будет отменено.

Это означает, что если параметр функции помечен как `mut`, то пока функция выполняется,
владелец не может читать или изменять ресурс.
Это так же означает, что `mut` не может быть передан в корутину или поток, 
то есть в scope, которые имеют другой жизненный цикл.

## Проверка аспектов

Аспекты владения проверяются с помощью системы типов во время компиляции.
Когда компилятор находит переменную или ресурс, он создаёт слот.
Слот это репрезентация ресурса. 
Слот принадлежит текущей области видимости (scope) и содержит информацию о текущих аспектах владения.

Следующий код, говорит компилятору:
```efen

let file: FileHandle

```

 - Создать слот с типом `FileHandle` и аспектом владения `own`, а так же доступом на чтение и запись.
 - Указать, что переменная `file` ссылается на этот слот.
 - Добавить слот в текущий scope.

Когда программист пишет код типа:
```efen

let file: FileHandle
let file2 = file

```

Компилятор не создаёт новый слот, а просто создаёт новую ссылку `file2`, 
которая ссылается на тот же слот, что и `file`.

Допустим мы вызываем функцию:
```efen
fn processFile(f1: FileHandle own, f2: FileHandle own) {
    // ...
}

let file: FileHandle
let file2 = file

processFile(file, file2)

```

Компилятор честно изменяет слот согласно спецификации вызова функции:
- он забирает право `own` из слота, и по сути помечает переменную `file` и `file2` как недействительные.
- когда он пытается сделать это второй раз для `file2`, он видит, что слот уже не имеет права `own`, 
  и выдаёт ошибку компиляции.

## Managed ownership (управляемое владение)

`Efen` поддерживает управляемое владение ресурсами через атрибут `managed`.

`managed` означает, что ресурсом владеет класс, структура или другой контейнер,
а не конкретная функция. Время жизни ресурса привязано к времени жизни владельца.

### Особенности managed ownership

- **Не требуется явная передача владения** - компилятор не проверяет transfer/consume
- **Программист управляет ресурсами** - создание, использование и удаление элементов
- **Автоматическое освобождение** - при уничтожении владельца (класс, структура)
- **Lifetime привязан к владельцу** - ресурс живёт, пока жив владелец

### Примеры использования

#### Logger с записями
```efen
class Logger {
    managed var records: [LogRecord]  // Владеет Logger

    fn add(record: LogRecord) {
        records.append(record)  // Программист управляет коллекцией
    }

    fn flush() {
        writeToFile(records)
        records.clear()  // Очистка под контролем программиста
    }

    deinit {
        // Автоматически освобождаются все LogRecord при уничтожении Logger
    }
}
```

#### Arena allocation
```efen
class Arena {
    managed var objects: [Object]

    fn allocate<T>(value: T) -> T* {
        let obj = Object(value)
        objects.append(obj)
        return &obj  // Lifetime привязан к Arena
    }

    deinit {
        // Все объекты освобождаются одновременно с Arena
    }
}
```

#### Глобальное состояние
```efen
managed var globalLogger: Logger?

fn initialize() {
    globalLogger = Logger()  // Присваивание без передачи владения
}

fn log(message: String) {
    globalLogger?.add(LogRecord(message))  // Используем без проверки ownership
}

// Logger будет существовать до конца программы или явного уничтожения
```

### Отличие от own

| Аспект            | `own`                           | `managed`                   |
|-------------------|---------------------------------|-----------------------------|
| Проверка transfer | ✓ Компилятор требует            | ✗ Не проверяется            |
| Ownership правила | Строгие (move, consume)         | Гибкие (программист решает) |
| Где используется  | Локальные переменные, параметры | Поля классов, глобальные    |
| Lifetime          | Scope функции                   | Lifetime владельца          |

### Когда использовать managed

- Коллекции объектов в классах/структурах
- Глобальное состояние и синглтоны
- Arena/pool allocators
- Кэши и буферы
- Любые ресурсы, управляемые контейнером

## Ссылочные параметры

`Efen` поддерживает передачу параметров по ссылке через префикс `&`. 
Ссылочные параметры позволяют функции изменять переменную вызывающей стороны без передачи владения.

### Синтаксис

```efen
fn modify(x: &Int) {
    // Изменяет переменную напрямую
}

fn initialize(x: &Int?) {
    // Инициализирует опциональную переменную
}
```

### Типы ссылочных параметров

| Тип       | Синтаксис   | Описание                                | Пример использования                   |
|-----------|-------------|-----------------------------------------|----------------------------------------|
| **inout** | `&T`        | Двунаправленный доступ: чтение и запись | Модификация существующего значения     |
| **out**   | `&T?`       | Только запись: инициализация переменной | Инициализация uninitialized переменной |

### Примеры использования

#### inout параметр
```efen
fn increment(value: &Int) {
    value = value + 1
}

var count = 5
increment(&count)  // count теперь 6
```

#### out параметр
```efen
fn tryParse(str: String, result: &Int?) -> Bool {
    if isValidNumber(str) {
        result = parseInt(str)
        return true
    }
    return false
}

var number: Int?
if tryParse("42", &number) {
    echo number  // 42
}
```

#### Комбинация с ownership модификаторами
```efen
fn processBuffer(data: &read write [Byte]) {
    // Может читать и писать в массив data
}

fn shareData(cache: &shared read [String]) {
    // Параллельное чтение из разных потоков
}
```

### Ограничения ссылочных параметров

**Функции не могут возвращать ссылки** - это запрещено для предотвращения dangling references:

```efen
// ❌ ОШИБКА: Функция не может вернуть ссылку
fn getBadReference() -> &Int {
    var local = 42
    return &local  // Dangling reference после выхода из функции
}
```

Компилятор выдаст ошибку:
```
error: Function 'getBadReference' cannot return a reference type to avoid dangling references
```

### Семантика ссылочных параметров

- **Время жизни**: Ссылка действительна только в scope вызова функции
- **Не владеет ресурсом**: Ссылка не может освободить переданный ресурс
- **Эксклюзивный доступ**: `&mut T` лишает вызывающую сторону доступа к ресурсу на время выполнения функции
- **Передача в корутины**: `&mut` нельзя передать в корутину/поток с другим жизненным циклом

### Разница между & и own

| Аспект                    | `&T` (ссылка)           | `own T` (владение)                |
|---------------------------|-------------------------|-----------------------------------|
| Владение                  | Не передаётся           | Передаётся полностью              |
| Деструктор                | Не может вызвать        | Может вызвать при выходе из scope |
| Возврат из функции        | ❌ Запрещено             | ✓ Разрешено                       |
| Lifetime                  | Scope вызова            | Scope владельца                   |
| Доступ вызывающей стороны | Заблокирован при `&mut` | Полностью потерян                 |